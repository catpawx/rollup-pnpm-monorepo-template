{
  "version": "2.0.0",
  "tasks": [
    // 执行npm命令，当preLaunchTask超出默认的npm命令时，需要在tasks.json中配置
    {
      "type": "shell",
      "command": "npm run build:dev",
      "group": "build",
      "label": "npm run build:dev"
    }
  ]
}
